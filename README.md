viewSynthesis.cpp contains implementation of basic requirements.

For 4 neigbor point, top right corner is (0, 0) in (s, t), so the viewing is reversed. 
For example, (x, y) =  (-119.5, 119.5). bl = 0, br = 1, tl = 9, and tr = 10.
